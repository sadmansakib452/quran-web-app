# Backend environment variables

# Port for the HTTP server.
PORT=8787

# CORS origin allowed to call this API from a browser.
# - Use "*" for quick local testing.
# - In production, set this to your deployed frontend origin (recommended).
CORS_ORIGIN=*

